# SentinelIQ — Frontend

Union360's Next.js banking app, with SentinelIQ's fraud-protection overlays built in. See the [repo root README](../README.md) for full setup instructions, demo login, and a walkthrough of every trust-score scenario.

```bash
npm install
npm run dev   # http://localhost:3000 — requires the backend running on :5000
```
